# 📘 Astro Content Collections 配置说明

本文档用于说明项目中 `src/content.config.ts` 文件的结构与作用。

---

## 一、文件作用

`src/content.config.ts` 是 Astro 的 **内容集合配置文件（Content Collections Config）**。  
它告诉 Astro 如何读取和验证 Markdown（或 JSON）等内容文件。

通过这个文件，Astro 能在构建时：
- 自动识别 `src/content/posts` 目录下的文章；
- 确保每篇文章的元数据字段格式正确；
- 在编辑器中提供类型提示（TypeScript 支持）。

---

## 二、主要模块

### 1. `defineCollection()`
定义一个内容集合（collection），这里定义了一个名为 **posts** 的集合，对应博客文章。

```ts
const posts = defineCollection({
  loader: glob({ pattern: "**/*.md", base: "./src/content/posts" }),
  schema: z.object({...})
});
